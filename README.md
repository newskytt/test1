<div align="center">
<img width="1200" height="475" alt="GHBanner" src="https://github.com/user-attachments/assets/0aa67016-6eaf-458a-adb2-6e31a0763ed6" />
</div>

# ZenAI Todo 应用

一个集成了 AI 辅助功能的智能待办事项管理应用，使用 React + TypeScript + Express.js + Supabase 构建。

## 功能特性

### 🎯 核心功能
- **用户认证**：不同用户拥有独立的待办列表
- **待办事项管理**：创建、编辑、删除、完成待办事项
- **子任务管理**：将复杂任务分解为可管理的子步骤
- **任务优先级**：自动为任务分配优先级和类别
- **进度跟踪**：实时显示任务完成进度
- **数据持久化**：数据保存在本地存储和 Supabase 数据库中

### 🤖 AI 辅助功能
- **任务优先级建议**：AI 分析任务内容，推荐合适的优先级和类别
- **任务分解**：将复杂任务自动分解为可执行的子步骤
- **励志名言**：随机获取励志名言，增强用户动力

### 📱 用户体验
- **响应式设计**：适配不同屏幕尺寸
- **流畅动画**：添加和完成任务时的平滑过渡效果
- **错误处理**：网络错误时自动切换到本地存储
- **即时反馈**：操作后立即更新界面

## 技术栈

### 前端
- React 19.2.4
- React DOM 19.2.4
- TypeScript 5.8.2
- Vite 6.2.0
- @google/genai 1.38.0

### 后端
- Node.js
- Express.js 4.18.2
- Supabase (PostgreSQL)
- CORS 2.8.5
- Dotenv 16.3.1

## 快速开始

### 前提条件
- Node.js 14+ 已安装
- Supabase 项目已创建
- Gemini API Key (可选，用于本地 AI 功能)

### 安装和运行

#### 1. 克隆项目
```bash
git clone <repository-url>
cd zenai-todo
```

#### 2. 配置前端

**安装依赖**
```bash
npm install
```

**设置环境变量**
创建 `.env.local` 文件并添加以下内容：
```env
GEMINI_API_KEY=your-gemini-api-key
```

**运行前端开发服务器**
```bash
npm run dev
```
前端服务将在 `http://localhost:3000` 上运行。

#### 3. 配置后端

**进入后端目录**
```bash
cd backend
```

**安装依赖**
```bash
npm install
```

**配置环境变量**
编辑 `.env` 文件，填写您的 Supabase 配置：
```env
# Supabase Configuration
SUPABASE_URL=your-supabase-url
SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# Server Configuration
PORT=3001
NODE_ENV=development
```

**运行后端服务**
```bash
npm run start
```
后端服务将在 `http://localhost:3001` 上运行。

## 项目结构

```
zenai-todo/
├── backend/             # 后端服务
│   ├── .env             # 后端环境变量
│   ├── index.js         # 后端入口文件
│   ├── package.json     # 后端依赖
│   └── README.md        # 后端文档
├── components/          # 前端组件
│   ├── PriorityBadge.tsx # 优先级徽章组件
│   └── TodoItem.tsx     # 待办事项组件
├── services/            # 前端服务
│   └── geminiService.ts # Gemini AI 服务
├── .gitignore           # Git 忽略文件
├── App.tsx              # 前端主应用
├── index.html           # HTML 入口
├── index.tsx            # React 入口
├── package.json         # 前端依赖
├── tsconfig.json        # TypeScript 配置
├── types.ts             # TypeScript 类型定义
├── vite.config.ts       # Vite 配置
└── README.md            # 项目文档
```

## API 文档

### 基础 URL
所有 API 端点的基础 URL 为：`http://localhost:3001/api`

### 用户相关 API

#### POST /api/auth/login
**功能**：用户登录或注册（如果用户不存在）

**请求体**：
```json
{
  "username": "string" // 用户名
}
```

**响应**：
```json
{
  "success": true,
  "user": {
    "id": "string", // 用户 ID
    "username": "string", // 用户名
    "created_at": "string" // 创建时间
  }
}
```

**错误响应**：
```json
{
  "error": "Username is required" // 错误信息
}
```

### 待办事项相关 API

#### GET /api/todos/:username
**功能**：获取指定用户的所有待办事项，包括子任务

**参数**：
- `username`: 用户名（路径参数）

**响应**：
```json
{
  "success": true,
  "todos": [
    {
      "id": "string", // 待办事项 ID
      "username": "string", // 用户名
      "text": "string", // 任务内容
      "completed": false, // 完成状态
      "priority": "string", // 优先级（高/中/低）
      "category": "string", // 类别
      "created_at": "string", // 创建时间
      "subTasks": [
        {
          "id": "string", // 子任务 ID
          "todo_id": "string", // 关联的待办事项 ID
          "text": "string", // 子任务内容
          "completed": false, // 完成状态
          "created_at": "string" // 创建时间
        }
      ]
    }
  ]
}
```

#### POST /api/todos
**功能**：创建新的待办事项

**请求体**：
```json
{
  "username": "string", // 用户名
  "text": "string", // 任务内容
  "priority": "string", // 优先级（高/中/低）
  "category": "string" // 类别
}
```

**响应**：
```json
{
  "success": true,
  "todo": {
    "id": "string", // 待办事项 ID
    "username": "string", // 用户名
    "text": "string", // 任务内容
    "completed": false, // 完成状态
    "priority": "string", // 优先级
    "category": "string", // 类别
    "created_at": "string" // 创建时间
  }
}
```

**错误响应**：
```json
{
  "error": "Username and text are required" // 错误信息
}
```

#### PUT /api/todos/:id
**功能**：更新待办事项的完成状态

**参数**：
- `id`: 待办事项 ID（路径参数）

**请求体**：
```json
{
  "completed": true // 完成状态
}
```

**响应**：
```json
{
  "success": true,
  "todo": {
    "id": "string", // 待办事项 ID
    "username": "string", // 用户名
    "text": "string", // 任务内容
    "completed": true, // 完成状态
    "priority": "string", // 优先级
    "category": "string", // 类别
    "created_at": "string" // 创建时间
  }
}
```

#### DELETE /api/todos/:id
**功能**：删除待办事项及其关联的子任务

**参数**：
- `id`: 待办事项 ID（路径参数）

**响应**：
```json
{
  "success": true
}
```

### 子任务相关 API

#### POST /api/subtasks
**功能**：创建新的子任务

**请求体**：
```json
{
  "todo_id": "string", // 关联的待办事项 ID
  "text": "string" // 子任务内容
}
```

**响应**：
```json
{
  "success": true,
  "subtask": {
    "id": "string", // 子任务 ID
    "todo_id": "string", // 关联的待办事项 ID
    "text": "string", // 子任务内容
    "completed": false, // 完成状态
    "created_at": "string" // 创建时间
  }
}
```

**错误响应**：
```json
{
  "error": "Todo ID and text are required" // 错误信息
}
```

#### PUT /api/subtasks/:id
**功能**：更新子任务的完成状态

**参数**：
- `id`: 子任务 ID（路径参数）

**请求体**：
```json
{
  "completed": true // 完成状态
}
```

**响应**：
```json
{
  "success": true,
  "subtask": {
    "id": "string", // 子任务 ID
    "todo_id": "string", // 关联的待办事项 ID
    "text": "string", // 子任务内容
    "completed": true, // 完成状态
    "created_at": "string" // 创建时间
  }
}
```

### AI 相关 API

#### POST /api/ai/suggest
**功能**：获取任务优先级和类别建议

**请求体**：
```json
{
  "text": "string" // 任务内容
}
```

**响应**：
```json
{
  "success": true,
  "suggestion": {
    "priority": "string", // 建议的优先级（高/中/低）
    "category": "string", // 建议的类别
    "reason": "string" // 建议理由
  }
}
```

**错误响应**：
```json
{
  "error": "Text is required" // 错误信息
}
```

#### POST /api/ai/decompose
**功能**：将任务分解为子步骤

**请求体**：
```json
{
  "text": "string" // 任务内容
}
```

**响应**：
```json
{
  "success": true,
  "steps": [
    "string", // 子步骤 1
    "string" // 子步骤 2
    // 更多子步骤...
  ]
}
```

**错误响应**：
```json
{
  "error": "Text is required" // 错误信息
}
```

#### GET /api/ai/quote
**功能**：获取励志名言

**响应**：
```json
{
  "success": true,
  "quote": "string" // 励志名言
}
```

## 本地存储

应用使用浏览器的本地存储作为数据备份，确保即使在网络错误或数据库连接失败的情况下，用户数据也不会丢失。

### 存储结构
- `zen-auth`: 存储当前登录用户信息
- `zen-todos-{username}`: 存储指定用户的待办事项列表

### 数据同步
- 当用户登录时，应用会先从本地存储加载数据，然后尝试从后端API获取最新数据
- 当用户添加、更新或删除待办事项时，数据会同时保存到本地存储和后端数据库
- 当网络错误时，应用会使用本地存储中的数据，确保用户体验不受影响

## 故障排除

### 常见问题

#### 1. Vite 代理错误 (ECONNREFUSED)
**原因**：后端服务未运行或端口被占用
**解决方法**：启动后端服务，确保端口 3001 未被占用

#### 2. Supabase 行级安全策略错误
**原因**：Supabase 服务角色密钥不正确或行级安全策略设置不当
**解决方法**：确保使用正确的服务角色密钥，并检查 Supabase 控制台中的行级安全策略设置

#### 3. AI 功能不工作
**原因**：Gemini API Key 未设置或无效
**解决方法**：在 `.env.local` 文件中设置有效的 Gemini API Key

#### 4. 数据丢失
**原因**：本地存储被清除或损坏
**解决方法**：重新登录，应用会尝试从后端数据库恢复数据

## 许可证

ISC 许可证

## 贡献

欢迎提交 Issue 和 Pull Request 来改进这个项目！

## 致谢

- React 团队
- Supabase 团队
- Google Gemini AI
- 所有贡献者

---

**享受使用 ZenAI Todo 应用！** 🎉